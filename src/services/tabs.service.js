import { domainFromUrl } from '../lib/util.js';export const tabsService={async activeDomain(){const [tab]=await chrome.tabs.query({active:true,currentWindow:true});return tab?.url?domainFromUrl(tab.url):'';},async allDomains(){const tabs=await chrome.tabs.query({});const set=new Set();for(const t of tabs){const d=t.url?domainFromUrl(t.url):'';if(d)set.add(d);}return [...set].sort();}};
