export class UITabs extends HTMLElement{connectedCallback(){this.classList.add('tabbar');this.buttons=Array.from(this.querySelectorAll('button'));this.buttons.forEach((b,i)=>b.addEventListener('click',()=>this.activate(i)));const init=Number(this.getAttribute('active')||0);this.activate(init);}activate(i){this.buttons.forEach((b,bi)=>b.classList.toggle('active',bi===i));this.dispatchEvent(new CustomEvent('change',{detail:{index:i}}));}};customElements.define('ui-tabs',UITabs);
